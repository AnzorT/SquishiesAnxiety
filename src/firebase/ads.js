import { TestIds } from 'react-native-google-mobile-ads';

// Google's public sample ad unit IDs — always serve test creatives and
// never earn revenue. Swap these for real AdMob ad unit IDs (and the real
// androidAppId in app.json) before shipping.
export const BANNER_AD_UNIT_ID = TestIds.BANNER;
export const REWARDED_AD_UNIT_ID = TestIds.REWARDED;
export const INTERSTITIAL_AD_UNIT_ID = TestIds.INTERSTITIAL;
